#ifndef WIFI_PARAM_H
#define WIFI_PARAM_H

#include <arduino.h>

#include "wifiConfigs.h"



const uint32_t connectTimeoutMs = INTERNET_TIME_OUT_MS;

const char* ssid1 = WIFI_SSID_1;
const char* password1 = WIFI_PASSWORD_1;
const char* ssid2 = WIFI_SSID_2;
const char* password2 = WIFI_PASSWORD_2;

#endif //WIFI_PARAM_H